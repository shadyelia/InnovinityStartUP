@model IEnumerable<Innovinity.Models.Clinics>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Arabic_Name)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Arabic_Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ClinicsID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ClinicsID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ClinicsID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
